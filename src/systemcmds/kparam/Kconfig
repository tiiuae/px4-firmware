menuconfig SYSTEMCMDS_KPARAM
	bool "param"
	default y
	depends on BOARD_PROTECTED && SYSTEMCMDS_PARAM
	---help---
		Enable support for param

config USER_KPARAM
	bool
	default n
	depends on BOARD_PROTECTED && SYSTEMCMDS_KPARAM
